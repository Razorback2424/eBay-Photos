# Readiness Evidence

Save JSON output from:

```bash
python3 scripts/capture_readiness.py --base-url https://your-domain.com
```

Keep the saved `/readiness` payloads from the final deployment candidate and the live launch deployment.
